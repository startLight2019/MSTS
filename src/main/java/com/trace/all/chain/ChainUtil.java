package com.trace.all.chain;

import java.util.ArrayList;

public class ChainUtil {
    /*在创世节点里面找到起始节点对应的数据和钱包*/
    public static void findStartNode(int id){
       /* ArrayList<Transaction> transactions = BlockChain.genesis.transactions;
        for (Transaction tr : transactions) {
            for (int tempId : BlockChain.nodeKeys.keySet()){
                if (BlockChain.nodeKeys.get(tempId) )
            }
            System.out.println(tr.reciepient+ " : " +tr.value);
        }*/
    }
}
